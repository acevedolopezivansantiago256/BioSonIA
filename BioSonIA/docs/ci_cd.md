# CI/CD

## GitHub Actions
- Define jobs de `lint`, `test`, `build` para `frontend`, `backend` y `ai`.
- Usa matrices de Node (18/20) y Python (3.10/3.11).
- Configura secretos: `GITHUB_TOKEN`, `DOCKER_USERNAME`, `DOCKER_PASSWORD`, `CLOUD_KEYS`.

## GitLab, Azure DevOps, Jenkins
- Plantillas incluidas en la raíz con etapas equivalentes.
- Ajusta runners/agents y variables de entorno.