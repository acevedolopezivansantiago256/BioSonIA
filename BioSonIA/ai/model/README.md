Este directorio contiene el clasificador y el mapa de clases.

- `bird_classifier.py`: carga YAMNet si está disponible; fallback a modelo toy.
- `class_map.csv`: mapeo de clases a especies de ejemplo.