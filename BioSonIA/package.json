{
  "name": "BioSonIA",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "setup": "echo 'Run scripts/setup.sh or setup.ps1'",
    "dev": "echo 'Use workspace dev scripts: frontend/backend/ai'",
    "build": "echo 'Use workspace build scripts'",
    "front": "npm run -w frontend dev",
    "front:p3010": "npm run -w frontend dev:p3010",
    "back": "npm run -w backend dev:demo",
    "py:setup": "cd ai && python -m venv .venv && .venv\\Scripts\\activate && pip install -r requirements.txt",
    "py": "cd ai && python server.py",
    "py:venv": "cd ai && .venv\\Scripts\\activate && python server.py"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}